# Huddle — Claude Code Instructions

When making changes, read ONLY the files mentioned in the prompt. Do not read unrelated files.

Key files:
- app/home.tsx — coach home screen
- app/practice.tsx — practice tab
- app/games.tsx — team tab
- app/chat.tsx — chat
- app/parent-home.tsx — parent home
- app/parent-team.tsx — parent team tab
- app/index.tsx — onboarding/auth
- lib/header.tsx — shared header
- lib/season.ts — season schedule data
- lib/roleStore.tsx — role context
